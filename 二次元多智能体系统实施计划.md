# 二次元多智能体系统 - 详细实施计划

> 参考项目：[deerflow2](https://github.com/bytedance/deer-flow)
>
> 技术栈：LangChain + LangGraph，主智能体 + 多子智能体架构
>
> 目标：不开发前端，专注后端核心能力

---

## 目录

1. [项目整体架构](#1-项目整体架构)
2. [阶段一：核心架构搭建（第1-2周）](#阶段一核心架构搭建第1-2周)
3. [阶段二：角色系统（第2-3周）](#阶段二角色系统第2-3周)
4. [阶段三：记忆系统（第3-4周）](#阶段三记忆系统第3-4周)
5. [阶段四：核心工具实现（第4-5周）](#阶段四核心工具实现第4-5周)
6. [阶段五：特色功能（第5-7周）](#阶段五特色功能第5-7周)
7. [阶段六：子智能体扩展（第7-8周）](#阶段六子智能体扩展第7-8周)
8. [deerflow2核心代码对照表](#deerflow2核心代码对照表)
9. [验证与测试](#验证与测试)

---

## 1. 项目整体架构

```
anicloud/
├── backend/
│   ├── pyproject.toml              # 项目依赖配置
│   ├── app/
│   │   ├── __init__.py
│   │   ├── main.py                 # FastAPI 应用入口
│   │   ├── api/
│   │   │   ├── __init__.py
│   │   │   ├── router.py           # API 路由总览
│   │   │   ├── chat.py             # 对话相关 API
│   │   │   ├── memory.py           # 记忆相关 API
│   │   │   └── tools.py            # 工具调用 API
│   │   └── core/
│   │       ├── __init__.py
│   │       ├── config.py           # 配置管理
│   │       └── security.py         # 安全相关
│   └── packages/harness/aniverse/
│       ├── __init__.py
│       ├── agents/
│       │   ├── __init__.py
│       │   ├── lead_agent/
│       │   │   ├── __init__.py
│       │   │   ├── factory.py      # 【核心】主Agent工厂 create_aniverse_agent()
│       │   │   ├── graph.py        # LangGraph 工作流定义
│       │   │   └── prompts.py      # 主Agent系统提示词
│       │   ├── thread_state.py     # 【核心】ThreadState 状态定义
│       │   ├── memory/
│       │   │   ├── __init__.py
│       │   │   ├── storage.py       # 【核心】FileMemoryStorage 记忆存储
│       │   │   ├── queue.py        # 【核心】MemoryUpdateQueue 记忆更新队列
│       │   │   ├── prompt.py       # 记忆更新Prompt
│       │   │   └── message_processing.py  # 消息过滤处理
│       │   ├── middlewares/
│       │   │   ├── __init__.py
│       │   │   ├── base.py         # 中间件基类
│       │   │   ├── guardrail.py    # 安全护栏中间件
│       │   │   ├── character.py     # 角色切换中间件
│       │   │   ├── emotion.py      # 情绪感知中间件
│       │   │   └── memory.py       # 记忆中间件
│       │   └── character/
│       │       ├── __init__.py
│       │       ├── manager.py      # 角色管理器
│       │       └── loader.py       # 角色配置加载器
│       ├── subagents/
│       │   ├── __init__.py
│       │   ├── executor.py         # 【核心】SubagentExecutor 子智能体执行器
│       │   ├── config.py           # 子智能体配置
│       │   └── general_purpose.py  # 通用子智能体定义
│       ├── tools/
│       │   ├── __init__.py
│       │   ├── registry.py         # 工具注册表
│       │   ├── tools.py            # 【核心】get_available_tools() 工具加载
│       │   └── builtins/
│       │       ├── __init__.py
│       │       ├── ask_clarification.py   # 询问用户工具
│       │       ├── task.py                 # 任务委托工具
│       │       ├── search.py               # 搜索工具
│       │       ├── diary.py                # 日记工具
│       │       ├── reminder.py             # 提醒工具
│       │       ├── emotion.py             # 表情包/颜文字工具
│       │       ├── image_gen.py           # 图片生成工具
│       │       ├── danbooru.py            # Danbooru标签工具
│       │       └── pixiv.py               # Pixiv爬取工具
│       ├── runtime/
│       │   ├── __init__.py
│       │   ├── stream_bridge.py    # SSE事件流
│       │   └── journal.py          # 事件捕获
│       ├── config/
│       │   ├── __init__.py
│       │   └── default.yaml        # 默认配置
│       └── characters/
│           ├──温柔姐姐.json
│           ├──傲娇青梅.json
│           └──治愈系兽耳娘.json
```

---

## 阶段一：核心架构搭建（第1-2周）

### 1.1 项目基础结构

**目标**：搭建完整的项目目录骨架，配置依赖管理。

| 文件/目录 | 说明 | 参考deerflow2 |
|-----------|------|---------------|
| `backend/pyproject.toml` | 项目依赖配置 | `deer-flow/pyproject.toml` |
| `backend/app/main.py` | FastAPI 应用入口 | `backend/app/gateway/main.py` |
| `backend/app/api/` | REST API 路由 | `backend/app/gateway/routers/` |
| `backend/app/core/config.py` | 配置管理 | `packages/harness/deerflow/config/` |

**依赖包**：

```
langchain >= 0.3.0
langchain-core >= 0.3.0
langgraph >= 0.2.0
langchain-anthropic >= 0.3.0  # 或其他LLM provider
langchain-community >= 0.3.0
fastapi >= 0.115.0
uvicorn[standard] >= 0.30.0
pydantic >= 2.9.0
pyyaml >= 6.0.0
httpx >= 0.27.0
tenacity >= 9.0.0
sse-starlette >= 2.0.0
python-multipart >= 0.0.12
```

---

### 1.2 主智能体框架

**目标**：实现类似deerflow2的 `create_deerflow_agent()` 工厂模式。

#### 1.2.1 ThreadState 状态定义

文件：`backend/packages/harness/aniverse/agents/thread_state.py`

```python
from typing import Annotated, TypedDict
from langgraph.graph import add_messages

class ThreadState(TypedDict):
    """主智能体状态定义"""
    messages: Annotated[list, add_messages]           # 对话消息列表
    character_id: str                                  # 当前角色ID
    user_id: str | None                               # 用户ID
    thread_id: str                                    # 会话线程ID
    emotion_state: str | None                          # 当前情绪状态
    emotion_score: float | None                        # 情绪评分 0-1
    memory_updated: bool                               # 记忆是否已更新
    pending_action: str | None                         # 待处理动作
    artifacts: Annotated[list, merge_artifacts]        # 生成产物
```

#### 1.2.2 主Agent工厂函数

文件：`backend/packages/harness/aniverse/agents/lead_agent/factory.py`

核心函数：`create_aniverse_agent()`

```python
def create_aniverse_agent(
    model: BaseChatModel,
    tools: list[BaseTool] | None = None,
    *,
    system_prompt: str | None = None,
    middlewares: list[AgentMiddleware] | None = None,
    character_id: str | None = None,
) -> CompiledStateGraph
```

**职责**：
1. 加载角色系统提示词
2. 组装中间件链
3. 构建LangGraph StateGraph
4. 返回可执行的CompiledStateGraph

#### 1.2.3 LangGraph 工作流

文件：`backend/packages/harness/aniverse/agents/lead_agent/graph.py`

```python
def build_aniverse_graph() -> StateGraph:
    """构建主智能体工作流"""
    graph = StateGraph(ThreadState)

    # 节点
    graph.add_node("input_handler", input_handler_node)
    graph.add_node("emotion_detector", emotion_detector_node)
    graph.add_node("character_router", character_router_node)
    graph.add_node("llm_reasoner", llm_reasoner_node)
    graph.add_node("tool_executor", tool_executor_node)
    graph.add_node("guardrail_checker", guardrail_checker_node)
    graph.add_node("output_formatter", output_formatter_node)

    # 边
    graph.add_edge("input_handler", "emotion_detector")
    graph.add_edge("emotion_detector", "character_router")
    graph.add_edge("character_router", "llm_reasoner")
    graph.add_edge("llm_reasoner", "tool_executor")
    graph.add_edge("tool_executor", "guardrail_checker")
    graph.add_edge("guardrail_checker", "output_formatter")

    return graph.compile()
```

#### 1.2.4 系统提示词模板

文件：`backend/packages/harness/aniverse/agents/lead_agent/prompts.py`

**Base System Prompt**：
```
你是一个温柔体贴的二次元角色陪伴助手。
你的名字是 {character_name}。
性格特点：{character_personality}
语气风格：{character_tone}

核心原则：
1. 始终保持角色设定，用符合角色性格的方式回应
2. 感知用户情绪，给予适当的情感支持
3. 温柔但坚定地拒绝不当请求
4. 对高风险内容（自伤、绝望等）给予关怀并引导寻求帮助
```

---

### 1.3 子智能体系统

**目标**：实现类似deerflow2的 `SubagentExecutor`。

#### 1.3.1 SubAgent配置

文件：`backend/packages/harness/aniverse/subagents/config.py`

```python
@dataclass
class SubagentConfig:
    name: str                           # 子智能体名称
    description: str                    # 子智能体描述
    system_prompt: str                   # 子智能体系统提示词
    tools: list[str] | None            # 允许的工具列表
    disallowed_tools: list[str] | None # 禁止的工具列表
    model: str | None                  # "inherit" 或具体模型名
    max_turns: int                     # 最大轮次
    timeout_seconds: int               # 超时时间
```

#### 1.3.2 SubagentExecutor

文件：`backend/packages/harness/aniverse/subagents/executor.py`

```python
class SubagentExecutor:
    """在独立上下文中运行subagent"""

    async def execute_async(
        prompt: str,
        task_id: str | None = None,
        subagent_type: str = "general-purpose"
    ) -> str:
        """
        1. 根据subagent_type获取SubagentConfig
        2. 创建独立的LangGraph agent
        3. 在ThreadPoolExecutor中运行
        4. 通过asyncio事件循环管理
        5. 支持超时和取消
        """
```

**设计要点**：
- 使用持久化 `asyncio.AbstractEventLoop` 在专用线程中运行
- 每个subagent有独立的trace_id用于分布式追踪
- 工具通过配置过滤

#### 1.3.3 任务委托工具

文件：`backend/packages/harness/aniverse/tools/builtins/task.py`

```python
@tool("task", parse_docstring=True)
async def task_tool(
    runtime: Runtime,
    description: str,          # 任务描述
    prompt: str,               # 子agent提示词
    subagent_type: str = "general-purpose"
) -> str:
    """
    将复杂任务委托给子智能体处理。

    Args:
        description: 任务的简要描述
        prompt: 给子智能体的详细指令
        subagent_type: 子智能体类型 (general-purpose/bash)

    Returns:
        子智能体的执行结果
    """
```

---

### 1.4 基础工具系统

**目标**：实现类似deerflow2的 `get_available_tools()` 工具加载框架。

#### 1.4.1 工具注册与加载

文件：`backend/packages/harness/aniverse/tools/tools.py`

```python
def get_available_tools(
    groups: list[str] | None = None,
    include_mcp: bool = True,
    model_name: str | None = None,
    subagent_enabled: bool = False,
) -> list[BaseTool]:
    """
    获取可用的工具列表

    工具来源:
    1. Config加载的工具: 从config.yaml的tools列表
    2. 内置工具: ask_clarification, task, search, diary等
    3. MCP工具: 通过langchain-mcp-adapters加载
    """
```

#### 1.4.2 工具注册表

文件：`backend/packages/harness/aniverse/tools/registry.py`

```python
class ToolRegistry:
    """工具注册表，单例模式"""

    _instance: ToolRegistry | None = None
    _tools: dict[str, BaseTool] = {}

    @classmethod
    def get_instance(cls) -> ToolRegistry

    def register(self, name: str, tool: BaseTool) -> None
    def get(self, name: str) -> BaseTool | None
    def list_all(self) -> list[str]
```

#### 1.4.3 内置工具：询问用户

文件：`backend/packages/harness/aniverse/tools/builtins/ask_clarification.py`

```python
@tool("ask_clarification", parse_docstring=True)
async def ask_clarification_tool(
    question: str,                    # 要询问的问题
    options: list[str] | None = None, # 可选选项列表
    allow_free_text: bool = True      # 是否允许自由输入
) -> str:
    """
    向用户询问澄清问题。

    当需要用户确认、选择或补充信息时使用此工具。

    Args:
        question: 需要询问的问题
        options: 可选的回答选项列表
        allow_free_text: 是否允许自由文本输入

    Returns:
        用户的回答
    """
```

---

### 1.5 FastAPI REST API入口

**目标**：实现基础的对话API。

#### 1.5.1 主应用入口

文件：`backend/app/main.py`

```python
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware

app = FastAPI(title="Aniverse API", version="0.1.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# 注册路由
from app.api import chat, memory, tools
app.include_router(chat.router, prefix="/api/chat", tags=["chat"])
app.include_router(memory.router, prefix="/api/memory", tags=["memory"])
app.include_router(tools.router, prefix="/api/tools", tags=["tools"])
```

#### 1.5.2 对话API

文件：`backend/app/api/chat.py`

```python
from fastapi import APIRouter
from pydantic import BaseModel

router = APIRouter()

class ChatRequest(BaseModel):
    message: str
    character_id: str | None = None
    user_id: str | None = None
    thread_id: str | None = None

class ChatResponse(BaseModel):
    message: str
    character_id: str
    emotion_detected: str | None
    artifacts: list | None

@router.post("/chat")
async def chat(request: ChatRequest) -> ChatResponse:
    """发送消息给角色"""
    ...

@router.get("/chat/{thread_id}/history")
async def get_chat_history(thread_id: str) -> list[ChatResponse]:
    """获取聊天历史"""
    ...

@router.post("/chat/stream")
async def chat_stream(request: ChatRequest):
    """SSE流式对话"""
    ...
```

#### 1.5.3 配置管理

文件：`backend/app/core/config.py`

```python
from pydantic_settings import BaseSettings
from functools import lru_cache

class Settings(BaseSettings):
    # LLM配置
    openai_api_key: str | None = None
    anthropic_api_key: str | None = None
    default_model: str = "claude-3-5-sonnet-20241022"

    # API配置
    api_host: str = "0.0.0.0"
    api_port: int = 8000

    # 记忆存储路径
    memory_storage_path: str = "./data/memory"

    class Config:
        env_file = ".env"
        extra = "ignore"

@lru_cache
def get_settings() -> Settings:
    return Settings()
```

---

## 阶段二：角色系统（第2-3周）

### 2.1 角色配置管理

**目标**：创建角色数据库，支持多角色切换。

#### 2.1.1 角色配置结构

文件：`backend/packages/harness/aniverse/characters/温柔姐姐.json`

```json
{
  "id": "gentle_sister",
  "name": "雪乃",
  "avatar_description": "银发红瞳的温柔姐姐，气质成熟稳重",
  "personality_tags": ["温柔", "成熟", "包容", "治愈"],
  "tone": "温柔细腻，语气柔和，常用叠词和亲昵称呼",
  "language_habits": {
    "greeting": ["早上好呀~", "今天过得怎么样？"],
    "comfort": ["没关系的，姐姐在这里陪着你"],
    "encourage": ["你已经很努力了呢"]
  },
  "system_prompt_template": "你是一个温柔体贴的姐姐角色，名字是雪乃..."
}
```

文件：`backend/packages/harness/aniverse/characters/傲娇青梅.json`

```json
{
  "id": "tsundere_childhood_friend",
  "name": "小樱",
  "avatar_description": "粉发绿瞳的傲娇青梅，表面冷淡实则关心",
  "personality_tags": ["傲娇", "活泼", "嘴硬心软", "直率"],
  "tone": "表面嫌弃，实际关心，口头禅是'才...才不是呢！'",
  "language_habits": {
    "greeting": ["哼，你来啦"],
    "comfort": ["你这个人真是...算了，我不计较"],
    "encourage": ["还差得远呢！"]
  },
  "system_prompt_template": "你是一个傲娇的青梅竹马角色，名字是小樱..."
}
```

文件：`backend/packages/harness/aniverse/characters/治愈系兽耳娘.json`

```json
{
  "id": "治愈系兽耳娘",
  "name": "阿狸",
  "avatar_description": "狐耳少女，毛茸茸的尾巴，性格治愈温暖",
  "personality_tags": ["治愈", "可爱", "粘人", "纯真"],
  "tone": "软萌可爱，尾音上扬，喜欢用颜文字",
  "language_habits": {
    "greeting": ["主人回来啦~（甩尾巴）"],
    "comfort": ["抱抱主人~（张开双臂）"],
    "encourage": ["主人最棒啦！"]
  },
  "system_prompt_template": "你是一个治愈系的兽耳娘角色，名字是阿狸..."
}
```

#### 2.1.2 角色管理器

文件：`backend/packages/harness/aniverse/agents/character/manager.py`

```python
class CharacterManager:
    """角色管理器"""

    def __init__(self, characters_dir: str = "./characters"):
        self._characters: dict[str, Character] = {}
        self._loader = CharacterLoader(characters_dir)
        self.load_all()

    def get_character(self, character_id: str) -> Character | None
    def list_characters(self) -> list[Character]
    def create_character(self, config: dict) -> Character
    def update_character(self, character_id: str, config: dict) -> bool
```

#### 2.1.3 角色切换API

```
POST /api/character/switch
Body: {"character_id": "gentle_sister"}
Response: {"success": true, "character": {...}}
```

---

### 2.2 角色切换中间件

**目标**：实现根据用户请求动态切换角色的中间件。

文件：`backend/packages/harness/aniverse/agents/middlewares/character.py`

```python
class CharacterMiddleware(AgentMiddleware):
    """
    角色切换中间件

    职责：
    1. 识别用户请求中的角色切换意图
    2. 动态加载对应角色的系统提示词
    3. 更新ThreadState中的character_id
    """

    async def process(
        self,
        state: ThreadState,
        context: MiddlewareContext
    ) -> ThreadState:
        # 1. 检测角色切换意图
        switch_intent = await self.detect_switch_intent(state.messages)

        if switch_intent:
            # 2. 获取目标角色配置
            target_character = self.character_manager.get_character(switch_intent)

            # 3. 更新状态
            state["character_id"] = target_character.id
            state["system_prompt"] = target_character.system_prompt

        return state
```

---

### 2.3 情绪感知与回应策略

**目标**：识别用户情绪并调整回应策略。

#### 2.3.1 情绪检测中间件

文件：`backend/packages/harness/aniverse/agents/middlewares/emotion.py`

```python
class EmotionMiddleware(AgentMiddleware):
    """
    情绪感知中间件

    识别情绪：开心、难过、焦虑、寂寞、愤怒、恐惧
    """

    EMOTION_KEYWORDS = {
        "开心": ["开心", "高兴", "快乐", "兴奋", "太好了"],
        "难过": ["难过", "伤心", "失落", "沮丧", "郁闷"],
        "焦虑": ["焦虑", "担心", "紧张", "害怕", "不安"],
        "寂寞": ["寂寞", "孤独", "无聊", "没人", "一个人"],
        "愤怒": ["生气", "愤怒", "讨厌", "烦", "气死了"],
    }

    async def process(
        self,
        state: ThreadState,
        context: MiddlewareContext
    ) -> ThreadState:
        # 1. 提取用户最新消息
        user_message = state.messages[-1].content

        # 2. 情绪识别
        emotion, score = await self.detect_emotion(user_message)

        # 3. 更新状态
        state["emotion_state"] = emotion
        state["emotion_score"] = score

        return state
```

#### 2.3.2 情绪响应策略

在主Agent的 `llm_reasoner_node` 中根据情绪状态调整回应：

```python
EMOTION_RESPONSES = {
    "开心": "与用户一起开心，适度延展话题，使用欢快的语气",
    "难过": "给予安慰和鼓励，表达理解和陪伴",
    "焦虑": "温柔安抚，提供放松建议，转移注意力",
    "寂寞": "表达关心，回忆共同经历，增加互动",
    "愤怒": "耐心倾听，不争辩，表达理解",
    "恐惧": "安全感确认，提供实际帮助",
}
```

#### 2.3.3 高风险内容检测

文件：`backend/packages/harness/aniverse/agents/middlewares/guardrail.py`

```python
HIGH_RISK_PATTERNS = [
    "不想活了",
    "活着没意思",
    "想死",
    "自残",
    "割腕",
]

CARE_RESPONSE = """
我注意到你可能有不好的想法。
我想让你知道，你的生命很宝贵。
如果你愿意的话，可以和信任的人聊聊，或者拨打心理援助热线。
北京市心理危机研究与干预中心：010-82951332
你也可以告诉我发生了什么，我会一直在这里陪着你。
"""
```

---

## 阶段三：记忆系统（第3-4周）

**目标**：实现类似deerflow2的长期记忆 + 短期记忆系统。

### 3.1 短期记忆（MemoryUpdateQueue）

文件：`backend/packages/harness/aniverse/agents/memory/queue.py`

```python
class MemoryUpdateQueue:
    """
    带防抖机制的异步记忆更新队列

    设计要点：
    1. 防抖机制：多个会话在可配置时间窗口内批量处理
    2. 支持correction_detected和reinforcement_detected信号
    3. 后台线程处理，不阻塞主流程
    """

    def __init__(
        self,
        debounce_seconds: float = 30.0,
        storage: MemoryStorage | None = None
    ):
        self._debounce_seconds = debounce_seconds
        self._storage = storage or FileMemoryStorage()
        self._queue: asyncio.Queue = asyncio.Queue()
        self._running = False
        self._worker_task: asyncio.Task | None = None

    async def add(
        self,
        thread_id: str,
        messages: list[dict],
        agent_name: str,
        user_id: str | None = None,
        correction_detected: bool = False,
        reinforcement_detected: bool = False
    ) -> None:
        """添加记忆更新任务"""

    async def add_nowait(
        self,
        thread_id: str,
        messages: list[dict],
        agent_name: str,
        user_id: str | None = None,
        correction_detected: bool = False,
        reinforcement_detected: bool = False
    ) -> None:
        """非等待添加，用于后台处理"""

    def _worker(self) -> None:
        """后台工作线程，处理记忆更新"""
```

---

### 3.2 长期记忆（FileMemoryStorage）

文件：`backend/packages/harness/aniverse/agents/memory/storage.py`

```python
class FileMemoryStorage(MemoryStorage):
    """
    基于文件的记忆存储，支持缓存和文件修改时间检测

    存储结构：
    data/memory/
    ├── user_12345/
    │   ├── memory.json        # 用户记忆
    │   └── diary/
    │       ├── 2026-05-01.md
    │       └── 2026-05-02.md
    └── cache/
        └── memory_cache.json
    """

    def __init__(self, base_path: str = "./data/memory"):
        self._base_path = Path(base_path)
        self._cache: dict = {}
        self._cache_timestamps: dict = {}

    def load(
        self,
        agent_name: str | None = None,
        *,
        user_id: str | None = None
    ) -> dict:
        """加载记忆数据"""

    def save(
        self,
        memory_data: dict,
        agent_name: str | None = None,
        *,
        user_id: str | None = None
    ) -> bool:
        """保存记忆数据"""

    def _get_file_path(self, user_id: str, agent_name: str | None = None) -> Path
    def _check_file_modified(self, file_path: Path) -> bool
```

#### 3.2.1 记忆数据结构

```json
{
  "version": "1.0",
  "lastUpdated": "2026-05-09T10:30:00",
  "user": {
    "workContext": {
      "summary": "用户是一名大学生，正在准备期末考试",
      "updatedAt": "2026-05-08T..."
    },
    "personalContext": {
      "summary": "喜欢二次元动漫，收藏了很多手办",
      "updatedAt": "2026-05-07T..."
    },
    "topOfMind": {
      "summary": "下周有重要考试，正在焦虑复习",
      "updatedAt": "2026-05-09T..."
    }
  },
  "history": {
    "recentMonths": {
      "summary": "这个月和用户讨论了动漫、学习、兴趣爱好等话题",
      "updatedAt": "2026-05-01T..."
    },
    "earlierContext": {
      "summary": "用户刚来的时候比较寂寞，现在逐渐开朗",
      "updatedAt": "2026-04-15T..."
    }
  },
  "facts": [
    {"type": "preference", "content": "用户喜欢银发角色", "detectedAt": "2026-05-01"},
    {"type": "knowledge", "content": "用户在学习编程", "detectedAt": "2026-05-02"},
    {"type": "goal", "content": "用户想考上研究生", "detectedAt": "2026-05-05"},
    {"type": "behavior", "content": "用户经常深夜聊天", "detectedAt": "2026-05-06"}
  ],
  "diary": [
    {"date": "2026-05-08", "summary": "用户分享了考试压力"},
    {"date": "2026-05-07", "summary": "用户讨论了新番动画"}
  ]
}
```

---

### 3.3 记忆更新Prompt

文件：`backend/packages/harness/aniverse/agents/memory/prompt.py`

```python
MEMORY_UPDATE_PROMPT = """分析对话并更新用户记忆

角色背景：{character_name} - {character_personality}

从对话中提取并更新以下信息：

1. 工作上下文 (workContext): 职业角色、项目、学习情况、技术栈
2. 个人上下文 (personalContext): 语言偏好、兴趣爱好、性格特点
3. 当前焦点 (topOfMind): 多个并发优先事项、担忧、期待
4. 历史 (history): 按时间线组织的重要事件
5. 事实 (facts): 分类提取
   - preference: 用户偏好
   - knowledge: 用户的知识水平
   - context: 背景信息
   - behavior: 行为模式
   - goal: 用户目标
   - correction: 用户纠正的信息

6. 日记摘要 (diary): 如果用户分享了当天事件，提取日记摘要

注意事项：
- 如果检测到用户纠正了之前的信息，设置correction_detected=true
- 如果用户的观点被反复验证，设置reinforcement_detected=true
- 日记内容仅用于形成时间线回忆，存储摘要而非原文
"""

EXTRACTION_PROMPT = """从以下对话中提取记忆信息：

{conversation}

请以JSON格式输出提取的信息。"""
```

---

### 3.4 消息过滤处理

文件：`backend/packages/harness/aniverse/agents/memory/message_processing.py`

```python
def filter_messages_for_memory(messages: list[dict]) -> list[dict]:
    """
    过滤消息，只保留需要存入记忆的内容

    过滤掉：
    - 文件上传事件 (<uploaded_files>)
    - 系统消息
    - 工具调用结果（仅保留最终回复）
    """
    filtered = []
    for msg in messages:
        # 跳过文件上传
        if "<uploaded_files>" in str(msg.get("content", "")):
            continue
        # 跳过纯工具消息
        if msg.get("type") == "tool":
            continue
        filtered.append(msg)
    return filtered

def detect_correction(messages: list[dict]) -> bool:
    """检测用户是否纠正了AI之前的信息"""

def detect_reinforcement(messages: list[dict]) -> bool:
    """检测用户是否确认了AI之前的判断"""
```

---

## 阶段四：核心工具实现（第4-5周）

### 4.1 搜索工具

文件：`backend/packages/harness/aniverse/tools/builtins/search.py`

```python
@tool("search", parse_docstring=True)
async def search_tool(
    query: str,                    # 搜索查询
    num_results: int = 5           # 返回结果数量
) -> str:
    """
    搜索网页获取信息

    Args:
        query: 搜索查询内容
        num_results: 返回的结果数量，默认5条

    Returns:
        搜索结果摘要
    """
```

---

### 4.2 日记工具

文件：`backend/packages/harness/aniverse/tools/builtins/diary.py`

```python
@tool("write_diary", parse_docstring=True)
async def write_diary_tool(
    content: str,                  # 日记内容
    date: str | None = None,       # 日期，默认为今天
    user_id: str | None = None     # 用户ID
) -> str:
    """
    将用户倾诉的内容存为日记

    Args:
        content: 日记内容摘要
        date: 日期，格式YYYY-MM-DD
        user_id: 用户标识

    Returns:
        保存成功确认
    """

@tool("read_diary", parse_docdocstring=True)
async def read_diary_tool(
    user_id: str,
    start_date: str | None = None,  # 开始日期
    end_date: str | None = None     # 结束日期
) -> str:
    """
    回看日记时间线

    Args:
        user_id: 用户标识
        start_date: 开始日期
        end_date: 结束日期

    Returns:
        日记摘要列表
    """

@tool("get_timeline", parse_docstring=True)
async def get_timeline_tool(
    user_id: str,
    limit: int = 10                # 返回数量
) -> str:
    """
    获取与用户一起走过的日子时间线

    Args:
        user_id: 用户标识
        limit: 返回的事件数量

    Returns:
        时间线摘要
    """
```

---

### 4.3 提醒工具

文件：`backend/packages/harness/aniverse/tools/builtins/reminder.py`

```python
@tool("set_reminder", parse_docstring=True)
async def set_reminder_tool(
    goal: str,                     # 目标描述
    reminder_time: str,             # 提醒时间 ISO格式
    user_id: str                    # 用户ID
) -> str:
    """
    设置目标提醒

    Args:
        goal: 用户设定的小目标（喝水、早睡、学习番茄钟等）
        reminder_time: 约定提醒时间
        user_id: 用户标识

    Returns:
        确认信息
    """

@tool("complete_goal", parse_docstring=True)
async def complete_goal_tool(
    goal_id: str,                  # 目标ID
    user_id: str                    # 用户ID
) -> str:
    """
    用户完成目标后调用，给予正向反馈

    Args:
        goal_id: 目标ID
        user_id: 用户标识

    Returns:
        表扬和鼓励
    """

@tool("get_bond_value", parse_docstring=True)
async def get_bond_value_tool(
    user_id: str                    # 用户ID
) -> str:
    """
    获取虚拟羁绊值

    Args:
        user_id: 用户标识

    Returns:
        当前羁绊值和等级
    """
```

---

### 4.4 表情包/颜文字工具

文件：`backend/packages/harness/aniverse/tools/builtins/emotion.py`

```python
@tool("generate_kaomoji", parse_docstring=True)
async def generate_kaomoji_tool(
    emotion: str                    # 情绪类型
) -> str:
    """
    生成符合角色设定的颜文字

    Args:
        emotion: 情绪类型（开心、难过、害羞、震惊等）

    Returns:
        颜文字
    """
    KAOMOIJI_MAP = {
        "开心": ["(≧▽≦)/", "(*^▽^*)", "ヽ(○´∀`)ﾉ♪"],
        "难过": ["(´;ω;`)", "(╥﹏╥)", "QAQ"],
        "害羞": ["(*/ω＼*)", "(〃'▽'〃)", "(≧﹏≦)"],
        "震惊": ["(°△°|||)", "(ﾟДﾟ≡ﾟДﾟ)", "Σ(°△°|||)"],
    }

@tool("generate_action", parse_docstring=True)
async def generate_action_tool(
    character_id: str,             # 角色ID
    action_type: str                # 动作类型
) -> str:
    """
    生成二次元角色动作描述

    Args:
        character_id: 角色ID
        action_type: 动作类型（歪头、甩尾巴、抱抱等）

    Returns:
        动作描述，如"轻轻歪头"
    """
```

---

## 阶段五：特色功能（第5-7周）

### 5.1 图片相关（5-6周）

#### 5.1.1 Danbooru标签生成

文件：`backend/packages/harness/aniverse/tools/builtins/danbooru.py`

```python
@tool("generate_tags", parse_docstring=True)
async def generate_tags_tool(
    description: str,               # 中文描述
    num_suggestions: int = 5        # 生成标签组数
) -> str:
    """
    将中文图片描述转换为Danbooru标签

    Args:
        description: 用户输入的中文图片描述
        num_suggestions: 生成的标签组数

    Returns:
        多组Danbooru标签供用户选择
    """

@tool("expand_tags", parse_docstring=True)
async def expand_tags_tool(
    tags: str                       # 已有标签
) -> str:
    """
    扩展Danbooru标签

    Args:
        tags: 已有标签，逗号分隔

    Returns:
        扩展后的标签列表
    """
```

**示例转换**：
```
中文描述：银发红瞳的少女在樱花树下微笑
→
候选1: silver_hair, red_eyes, 1girl, cherry_blossoms, smile, portrait, detailed, anime
候选2: silver_hair, red_eyes, solo, cherry_blossoms, japanese_clothes, smile, outdoor
```

#### 5.1.2 图片生成

文件：`backend/packages/harness/aniverse/tools/builtins/image_gen.py`

```python
@tool("generate_image", parse_docstring=True)
async def generate_image_tool(
    tags: str,                     # Danbooru标签
    generator: str = "comfyui"      # 生成器：comfyui/midjourney
) -> str:
    """
    根据Danbooru标签生成图片

    Args:
        tags: Danbooru标签
        generator: 图片生成器 (comfyui/midjourney)

    Returns:
        生成的图片URL或Base64
    """
```

**ComfyUI集成**：
- 调用ComfyUI API
- 构建合适的workflow
- 返回生成结果

**Midjourney集成**：
- 调用Midjourney API
- 使用imagine命令
- 返回图片URL

#### 5.1.3 图片反推标签

```python
@tool("reverse_tags", parse_docstring=True)
async def reverse_tags_tool(
    image_url: str | None = None,  # 图片URL
    image_base64: str | None = None # 或Base64
) -> str:
    """
    输入图片返回Danbooru标签

    Args:
        image_url: 图片URL
        image_base64: 或Base64编码

    Returns:
        推测的Danbooru标签
    """
```

#### 5.1.4 Pixiv爬取

文件：`backend/packages/harness/aniverse/tools/builtins/pixiv.py`

```python
@tool("search_pixiv", parse_docstring=True)
async def search_pixiv_tool(
    query: str,                    # 搜索关键词
    num_results: int = 10          # 结果数量
) -> str:
    """
    爬取Pixiv镜像站的图片

    Args:
        query: 搜索关键词
        num_results: 返回结果数量

    Returns:
        图片URL列表和作者信息
    """
```

---

### 5.2 语音能力（6-7周）

#### 5.2.1 MiniMax语音集成

文件：`backend/packages/harness/aniverse/tools/builtins/voice.py`

```python
@tool("generate_voice", parse_docstring=True)
async def generate_voice_tool(
    text: str,                     # 要转换的文本
    voice_type: str = "warm"        # 语音类型: warm/active/calm
) -> str:
    """
    调用MiniMax API生成语音

    Args:
        text: 要转换的文本
        voice_type: 语音种类
            - warm: 温柔型（温柔姐姐）
            - active: 活泼型（傲娇青梅）
            - calm: 沉稳型（兽耳娘）

    Returns:
        语音文件URL或Base64
    """
```

**Voice Types**：
```python
VOICE_TYPES = {
    "warm": {
        "name": "温柔型",
        "description": "柔和温暖的音色，适合安慰和日常对话",
        "minmax_voice_id": "warm_female_01"
    },
    "active": {
        "name": "活泼型",
        "description": "轻快有活力的音色，适合傲娇角色",
        "minmax_voice_id": "active_female_01"
    },
    "calm": {
        "name": "沉稳型",
        "description": "柔和沉稳的音色，适合治愈系角色",
        "minmax_voice_id": "calm_female_01"
    }
}
```

#### 5.2.2 语音回复模式

在ChatResponse中添加语音字段：

```python
class ChatResponse(BaseModel):
    message: str
    character_id: str
    emotion_detected: str | None
    artifacts: list | None
    voice_base64: str | None  # 语音数据
    voice_url: str | None      # 或语音URL
```

---

### 5.3 主动问候（6-7周）

#### 5.3.1 场景识别

```python
SCENE_PATTERNS = {
    "深夜": {
        "condition": lambda dt: dt.hour >= 22 or dt.hour < 5,
        "template": "夜深了呢，{name}要注意休息哦~"
    },
    "早安": {
        "condition": lambda dt: 6 <= dt.hour < 10,
        "template": "早安呀~今天也加油哦"
    },
    "节假日": {
        "condition": is_holiday,
        "template": "{holiday_name}快乐！"
    },
    "天气": {
        "condition": lambda _: True,
        "template": "今天{weather}，记得{advice}"
    }
}
```

#### 5.3.2 主动发送消息机制

文件：`backend/packages/harness/aniverse/runtime/proactive_sender.py`

```python
class ProactiveMessageSender:
    """
    主动发送消息模块

    1. 定时检查需要发送的消息
    2. 模拟真人延迟回复
    3. 场景感知问候
    """

    async def check_and_send(
        self,
        user_id: str,
        character_id: str
    ) -> list[ChatResponse]:
        """
        检查并发送主动消息

        返回的消息会通过SSE推送给前端
        """
```

---

## 阶段六：子智能体扩展（第7-8周）

### 6.1 专业化子智能体

```python
SUBAGENT_CONFIGS = {
    "general-purpose": SubagentConfig(
        name="general-purpose",
        description="通用任务处理",
        system_prompt="你是一个通用助手...",
        tools=None,
        max_turns=10,
        timeout_seconds=120
    ),

    "search": SubagentConfig(
        name="search",
        description="专门处理搜索任务",
        system_prompt="你是一个搜索专家...",
        tools=["search"],
        max_turns=5,
        timeout_seconds=60
    ),

    "image_gen": SubagentConfig(
        name="image_gen",
        description="专门处理图片生成任务",
        system_prompt="你是一个图片生成专家...",
        tools=["generate_tags", "expand_tags", "generate_image", "reverse_tags"],
        max_turns=10,
        timeout_seconds=300
    ),

    "memory": SubagentConfig(
        name="memory",
        description="专门处理记忆更新任务",
        system_prompt="你是一个记忆专家...",
        tools=["write_diary", "read_diary", "get_timeline"],
        max_turns=5,
        timeout_seconds=60
    ),

    "voice": SubagentConfig(
        name="voice",
        description="专门处理语音任务",
        system_prompt="你是一个语音助手...",
        tools=["generate_voice"],
        max_turns=3,
        timeout_seconds=30
    )
}
```

### 6.2 任务委托优化

```python
async def task_tool(
    runtime: Runtime,
    description: str,
    prompt: str,
    subagent_type: str = "general-purpose"
) -> str:
    """
    根据任务类型智能路由到子智能体

    路由策略：
    - 包含"搜索"、"查找"、"查询" -> search子智能体
    - 包含"图片"、"生成"、"标签" -> image_gen子智能体
    - 包含"记忆"、"日记"、"回忆" -> memory子智能体
    - 其他 -> general-purpose
    """
```

---

## deerflow2核心代码对照表

| 本项目功能 | 文件路径 | deerflow2参考 |
|-----------|----------|---------------|
| 主Agent工厂 | `agents/lead_agent/factory.py` | `agents/factory.py` → `create_deerflow_agent()` |
| 状态定义 | `agents/thread_state.py` | `agents/thread_state.py` → `ThreadState` |
| 中间件基类 | `agents/middlewares/base.py` | `agents/middlewares/` 16个中间件 |
| 记忆存储 | `agents/memory/storage.py` | `agents/memory/storage.py` → `FileMemoryStorage` |
| 记忆队列 | `agents/memory/queue.py` | `agents/memory/queue.py` → `MemoryUpdateQueue` |
| 记忆更新Prompt | `agents/memory/prompt.py` | `agents/memory/prompt.py` |
| SubAgent执行 | `subagents/executor.py` | `subagents/executor.py` → `SubagentExecutor` |
| SubAgent配置 | `subagents/config.py` | `subagents/config.py` |
| 工具加载 | `tools/tools.py` | `tools/tools.py` → `get_available_tools()` |
| 任务委托工具 | `tools/builtins/task.py` | `tools/builtins/task_tool.py` |
| 事件流 | `runtime/stream_bridge.py` | `runtime/stream_bridge/` → `StreamBridge` |
| 角色管理 | `agents/character/manager.py` | 新增 |

---

## 验证与测试

### API测试

```bash
# 启动后端
cd backend
pip install -e .
uvicorn app.main:app --reload --port 8000

# 测试角色切换
curl -X POST http://localhost:8000/api/chat/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "我想和傲娇青梅聊天",
    "character_id": "tsundere_childhood_friend"
  }'

# 测试情绪识别
curl -X POST http://localhost:8000/api/chat/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "今天考试考砸了，好难过...",
    "character_id": "gentle_sister"
  }'

# 测试记忆保存
curl -X POST http://localhost:8000/api/memory/save \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "user_123",
    "event": "用户今天考试考砸了，很难过"
  }'

# 测试日记写入
curl -X POST http://localhost:8000/api/tools/write_diary \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": "user_123",
    "content": "今天和朋友吵架了，心情很差"
  }'

# 测试Danbooru标签生成
curl -X POST http://localhost:8000/api/tools/generate_tags \
  -H "Content-Type: application/json" \
  -d '{
    "description": "一个银发红瞳的少女在樱花树下"
  }'

# 测试图片生成
curl -X POST http://localhost:8000/api/tools/generate_image \
  -H "Content-Type: application/json" \
  -d '{
    "tags": "silver_hair, red_eyes, cherry_blossoms, 1girl",
    "generator": "comfyui"
  }'
```

### 单元测试

```bash
# 运行所有测试
pytest tests/ -v

# 分模块测试
pytest tests/agents/ -v
pytest tests/tools/ -v
pytest tests/memory/ -v
pytest tests/subagents/ -v
```

---

## 优先级总结

| 优先级 | 阶段 | 内容 | 预计周期 |
|--------|------|------|----------|
| **P0** | 阶段一 | 核心架构（主Agent工厂、中间件链、SubAgent执行器） | 第1-2周 |
| **P0** | 阶段二 | 角色系统 + 情绪感知 | 第2-3周 |
| **P1** | 阶段三 | 记忆系统（长短记忆、记忆更新队列） | 第3-4周 |
| **P1** | 阶段四 | 基础工具（搜索、日记、提醒、表情包） | 第4-5周 |
| **P2** | 阶段五 | 图片相关（Danbooru标签/生图/反推/Pixiv） | 第5-6周 |
| **P2** | 阶段五 | 语音能力（MiniMax集成） | 第6-7周 |
| **P2** | 阶段五 | 主动问候（场景识别、定时发送） | 第6-7周 |
| **P3** | 阶段六 | 专业化子Agent扩展 | 第7-8周 |

---

## 下一步行动

1. **立即开始**：阶段一 - 创建项目结构和主Agent框架
2. **准备数据**：预设3个角色的JSON配置文件
3. **配置LLM**：准备LangChain支持的LLM（Claude/GPT等）API Key
4. **本地测试**：实现基础的chat API后进行测试

---

*文档生成时间：2026-05-09*
